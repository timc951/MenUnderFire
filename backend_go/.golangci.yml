version: "2"

linters:
  default: standard
  # standard = errcheck, govet, ineffassign, staticcheck, unused

formatters:
  enable:
    - gofmt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0
